# getrandom 0.3+ requires an explicit backend on wasm. Scoped to the wasm target
# so native Linux/Android/Windows/macOS builds are untouched.
[target.wasm32-unknown-unknown]
rustflags = ['--cfg', 'getrandom_backend="wasm_js"']
