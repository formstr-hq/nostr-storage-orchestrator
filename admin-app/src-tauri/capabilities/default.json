{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Allow the main window to invoke registered core commands only",
  "windows": ["main"],
  "platforms": ["linux", "android"],
  "permissions": ["core:default"]
}
