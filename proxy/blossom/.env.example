BLOSSOM_SERVERS=http://localhost:3000
DB_API_URL=http://localhost:4000
# Optional: public origin used to build the `url` field of Blob Descriptors
# (BUD-02). Defaults to the request's own protocol/host if unset.
PUBLIC_BASE_URL=http://localhost:3001